@model KutuphaneMVC.Models.Loan
@{
    ViewData["Title"] = "Yeni Ödünç Ver";
}

<div class="container mt-5">
    <div class="card shadow-lg border-0 rounded-4 w-50 mx-auto">
        <div class="card-header text-white text-center"
             style="background: linear-gradient(90deg, #009688, #4db6ac, #80cbc4);">
            <h4 class="fw-bold mb-0">📦 @ViewData["Title"]</h4>
        </div>

        <div class="card-body p-4">
            <div asp-validation-summary="All" class="text-danger mb-3"></div>

            <form asp-action="Create" method="post">
                @Html.AntiForgeryToken()

                <div class="mb-3">
                    <label asp-for="MemberId" class="form-label fw-semibold text-dark">Üye</label>
                    <select asp-for="MemberId" class="form-select shadow-sm border-0 bg-light" asp-items="ViewBag.Members">
                        <option value="">-- Üye Seçiniz --</option>
                    </select>
                    <span asp-validation-for="MemberId" class="text-danger small"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="BookId" class="form-label fw-semibold text-dark">Kitap</label>
                    <select asp-for="BookId" class="form-select shadow-sm border-0 bg-light" asp-items="ViewBag.Books">
                        <option value="">-- Kitap Seçiniz --</option>
                    </select>
                    <span asp-validation-for="BookId" class="text-danger small"></span>
                </div>

                <div class="row">
                    <div class="col-md-6 mb-3">
                        <label asp-for="BorrowDate" class="form-label fw-semibold text-dark">Alış Tarihi</label>
                        <input asp-for="BorrowDate" type="date" class="form-control shadow-sm border-0 bg-light" />
                        <span asp-validation-for="BorrowDate" class="text-danger small"></span>
                    </div>

                    <div class="col-md-6 mb-3">
                        <label asp-for="DueDate" class="form-label fw-semibold text-dark">Teslim Tarihi</label>
                        <input asp-for="DueDate" type="date" class="form-control shadow-sm border-0 bg-light" />
                        <span asp-validation-for="DueDate" class="text-danger small"></span>
                    </div>
                </div>

                <div class="d-flex justify-content-between mt-4">
                    <a asp-action="Index" class="btn btn-outline-secondary px-4 py-2 rounded-3">⬅ Geri</a>
                    <button type="submit" class="btn btn-turquoise px-4 py-2 rounded-3 fw-semibold shadow-sm">💾 Kaydet</button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}

