@model KutuphaneMVC.Models.Member
@{
    ViewData["Title"] = "Üye Düzenle";
}

<div class="container mt-5">
    <div class="card shadow-lg border-0 rounded-4 w-50 mx-auto">
        <div class="card-header text-white text-center"
             style="background: linear-gradient(90deg, #009688, #4db6ac, #80cbc4);">
            <h4 class="fw-bold mb-0">✏️ @ViewData["Title"]</h4>
        </div>

        <div class="card-body p-4">
            <form asp-action="Edit" method="post">
                <input type="hidden" asp-for="MemberId" />

                <div class="mb-3">
                    <label asp-for="FullName" class="form-label fw-semibold text-dark">Ad Soyad</label>
                    <input asp-for="FullName" class="form-control shadow-sm border-0 bg-light" />
                </div>

                <div class="mb-3">
                    <label asp-for="Phone" class="form-label fw-semibold text-dark">Telefon</label>
                    <input asp-for="Phone" class="form-control shadow-sm border-0 bg-light" />
                </div>

                <div class="mb-3">
                    <label asp-for="Email" class="form-label fw-semibold text-dark">E-posta</label>
                    <input asp-for="Email" class="form-control shadow-sm border-0 bg-light" />
                </div>

                <div class="d-flex justify-content-between mt-4">
                    <a asp-action="Index" class="btn btn-outline-secondary px-4 py-2 rounded-3">⬅ İptal</a>
                    <button type="submit" class="btn btn-turquoise px-4 py-2 rounded-3 fw-semibold shadow-sm">💾 Kaydet</button>
                </div>
            </form>
        </div>
    </div>
</div>

