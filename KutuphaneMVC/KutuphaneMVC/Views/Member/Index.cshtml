@model IEnumerable<KutuphaneMVC.Models.Member>
@{
    ViewData["Title"] = "Üye Listesi";
}

<div class="container mt-5">
    <h2 class="page-title mb-4 text-center">👥 @ViewData["Title"]</h2>

    <div class="d-flex justify-content-between mb-3">
        <a asp-action="Create" class="btn btn-turquoise fw-semibold shadow-sm">➕ Yeni Üye Ekle</a>

        <form method="get" asp-action="Index" class="d-flex gap-2">
            <input type="text" name="search" class="form-control" placeholder="İsim veya e-posta ara..." />
            <button type="submit" class="btn btn-outline-turquoise">🔍 Ara</button>
        </form>
    </div>

    <div class="card shadow-sm border-0">
        <table class="table table-hover align-middle text-center mb-0">
            <thead class="table-turquoise text-white">
                <tr>
                    <th>Ad Soyad</th>
                    <th>Telefon</th>
                    <th>E-posta</th>
                    <th>İşlemler</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var uye in Model)
                {
                    <tr>
                        <td>@uye.FullName</td>
                        <td>@uye.Phone</td>
                        <td>@uye.Email</td>
                        <td>
                            <a asp-action="Edit" asp-route-id="@uye.MemberId" class="btn btn-sm btn-outline-turquoise">✏️</a>
                            <a asp-action="Delete" asp-route-id="@uye.MemberId" class="btn btn-sm btn-outline-danger">🗑️</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

    @if (!Model.Any())
    {
        <div class="text-center text-muted mt-4">
            <p>📭 Kayıtlı üye bulunamadı.</p>
        </div>
    }
</div>

